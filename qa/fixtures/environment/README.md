# QA environment directory

This directory is intentionally free of `.env` files. The Phase 1 launcher points Vite here and supplies only fixed synthetic variables through an explicit environment allowlist.
